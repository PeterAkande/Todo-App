import 'package:flutter/material.dart';

class DeskTopViewsSeparator extends StatelessWidget {
  const DeskTopViewsSeparator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 0.5,
      color: Colors.black,
    );
  }
}
